{
  // Editor entry point for client files, so the language server scopes them with
  // DOM libs (not the root Worker tsconfig). The Angular build uses tsconfig.app.json.
  "extends": "./tsconfig.app.json"
}
